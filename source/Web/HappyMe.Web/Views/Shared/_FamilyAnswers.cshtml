@model IEnumerable<HappyMe.Web.ViewModels.Questions.AnswerViewModel>

@foreach (var answer in Model)
{
    <div class="col-sm-3 text-center">
        <div class="color-answer" data-answer-id="@answer.Id">
            <img src="#" alt="@answer.Text" />
        </div>
    </div>
}
